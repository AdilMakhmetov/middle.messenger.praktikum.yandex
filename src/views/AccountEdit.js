import template from '../templates/AccountEdit.template';

export default class AccountEdit {
  constructor() {
    this.data = this.createData();
    this.template = template;
  }

  createData() {
    return ({});
  }
}
